# ClickHouse Connection Settings
DB_HOST=localhost
DB_PORT=8443
DB_NAME=testdb
DB_USER=default
DB_PASSWORD=your_password_here

# Security Settings
DB_SECURE=True

# Table Management
# Set to True to replace existing tables, False to create if not exists
REPLACE_FLAG=False
